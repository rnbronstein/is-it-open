// $(function(){
//   $(".form-search-by-type").submit(function(e){
//     e.preventDefault
//     alert('<%= @latitude %>')
//     debugger
//   })
// })

$(function(){
  submitForm()
})


function submitForm(){
  $(".form-search-by-type").submit(function(e){
    e.preventDefault
    alert("search was called")
    var searchType = $('input#search-type').val()
    var searchKeyword = $('input#search-keyword').val()
  })
  sendData(searchType, searchKeyword)
  debugger
}

function sendData(type, keyword){
  var data = {
    'type': type,
    'keyword': keyword
  }
  $.ajax({
    url: <%= search_path %>,
    method: "POST",
    dataType: "json",
    data: JSON.stringify(data),
    success: handleResponse(response_string),
    error: alert("AJAX error")
  })
}

// function handleResponse(responseString){
//
// }
